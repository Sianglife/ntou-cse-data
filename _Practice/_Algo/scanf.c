#include<stdio.h>

int main() {
    // int a, b, status;
    // while(status = scanf("%d, %d", &a, &b)) {
    //     printf("status: %d\n", status);
    // }

    char str[15];
    scanf("%14s", str);
    printf("%s", str);
}