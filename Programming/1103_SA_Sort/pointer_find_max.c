#include <stdio.h>

/**
 * @brief ?????È`?N?b?}?C???M???j??
 * @param arr ???V?}?C?}?Y??????
 * @param size ?}?C?????????
 * @return int ?}?C??????j??
 */
int find_max(int *arr, int size) {
    int max = -__INT_MAX__;
    for(int i=0; i < size; i++) {
        if (*(arr + i) > max) {
            max = *(arr + i);
        }
    }
    return max;
}

int main() {
    int numbers[6] = {10, 5, 100, 30, 50, 25};
    
    // ?I?s?æÌ
    int max_val = find_max(numbers, 6);
    
    printf("?}?C {10, 5, 100, 30, 50, 25} ??????j??O: %d\n", max_val);
    // ?w????X: 100
    
    int numbers2[4] = {-5, -2, -10, -1};
    max_val = find_max(numbers2, 4);
    printf("?}?C {-5, -2, -10, -1} ??????j??O: %d\n", max_val);
    // ?w????X: -1
    
    return 0;
}